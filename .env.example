# Copy to .env locally. Never commit secrets.
# Milestone 0: unused. Values are filled starting M1 / M13.

DATABASE_URL=postgresql+psycopg://marginmind:marginmind@localhost:5432/marginmind

# LLM: gemini = Google Gemini API (free tier, MVP live provider).
# stub = deterministic fallback. Do not integrate Gemini in M0 / M1.
LLM_PROVIDER=stub
GEMINI_API_KEY=

# Razorpay Test Mode (M13+)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
